<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.JobFlow" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.JobFlow" parent="Base.Theme.JobFlow" />
    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/float_top</item>
        <item name="android:windowExitAnimation">@anim/float_bottom</item>
    </style>
    <style name="DialogNavBarAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@anim/slide_out_right</item>
    </style>
</resources>